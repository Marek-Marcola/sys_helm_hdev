helm hdev
=========

Helm chart development tools.

Install
-------
Install:

    hdev.sh -inst -x
    -- or --
    hdev.sh -anpb -x
    -- or --
    cp -fv hdev.env /usr/local/etc
    cp -fv hdev.sh /usr/local/bin

Verify:

    hdev.sh -ver

Help:

    hdev.sh -h
